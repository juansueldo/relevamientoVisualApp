import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.relevamientovisualapp',
  appName: 'relevamientovisualApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
